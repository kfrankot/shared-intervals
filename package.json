{
  "name": "shared-interval",
  "version": "0.0.1",
  "description": "window.setInteval except duplicate delays share the same interval",
  "files": [
    "dist/"
  ],
  "main": "dist/index.js",
  "module": "dist/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "npm run test -- --watch",
    "types:check": "tsc --project tsconfig.json",
    "format:check": "prettier \"**/*\" --check --ignore-unknown",
    "format:fix": "npm run format:check -- --write",
    "lint": "eslint . --ext ts --max-warnings 0",
    "check": "npm run types:check && npm run format:check && npm run lint",
    "prebuild": "npx rimraf -rf dist",
    "build": "tsc --project tsconfig.production.json && rollup --config",
    "prepack": "npm run build"
  },
  "keywords": [
    "interval",
    "setInterval",
    "shared",
    "synchronize"
  ],
  "author": "Kevin Frankot",
  "license": "ISC",
  "devDependencies": {
    "@babel/plugin-transform-nullish-coalescing-operator": "^7.24.7",
    "@babel/plugin-transform-optional-chaining": "^7.24.7",
    "@babel/plugin-transform-runtime": "^7.24.7",
    "@babel/preset-env": "^7.24.7",
    "@babel/preset-typescript": "^7.24.7",
    "@rollup/plugin-babel": "^6.0.4",
    "@rollup/plugin-terser": "^0.4.4",
    "@types/jest": "^29.5.12",
    "eslint": "^9.6.0",
    "jest": "^29.7.0",
    "prettier": "^3.3.2",
    "rollup": "^4.18.0",
    "rollup-plugin-includepaths": "^0.2.4",
    "rollup-plugin-license": "^3.5.2",
    "ts-jest": "^29.1.5",
    "typescript": "^5.5.3"
  }
}
